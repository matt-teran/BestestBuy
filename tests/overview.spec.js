// import React from 'react';
// import Enzyme, { shallow } from 'enzyme';
// import Adapter from '@wojtekmaj/enzyme-adapter-react-17';
// import ProductTitle from '../src/components/overview/Product information/ProductTitle';
// import ProductDetail from '../src/components/overview/Product information/ProductDetail';

// Enzyme.configure({ adapter: new Adapter() });

// describe('Overview unit test', () => {
//   it('should have title element', () => {
//     const titleInstance = shallow(<ProductTitle />);
//     const element = titleInstance.find('div');
//     expect(element.hasClass('title')).toEqual(true);
//   });
//   it('should have product detail element', () => {
//     const detailInstance = shallow(<ProductDetail />);
//     const element = detailInstance.find('div');
//     expect(element.hasClass('product-detail')).toEqual(true);
//   });
// });
